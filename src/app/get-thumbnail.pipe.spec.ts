import { GetThumbnailPipe } from './get-thumbnail.pipe';

describe('GetThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new GetThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
