import { GetTotalPlusDeliveryPipe } from './get-total-plus-delivery.pipe';

describe('GetTotalPlusDeliveryPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTotalPlusDeliveryPipe();
    expect(pipe).toBeTruthy();
  });
});
